# Trivy Ignore File
# CVEs listed here will be ignored during Trivy scans

# CVE-2026-26996 - minimatch ReDoS vulnerability
# Affected: minimatch < 10.2.1 (transitive dependency via aws-cdk-lib)
# Justification: aws-cdk-lib is a dev-only dependency, not in production runtime
# Risk: ReDoS requires crafted glob patterns, only affects build/deploy processes
# Status: Waiting for aws-cdk-lib to update minimatch dependency
# Review: Check periodically with 'npm view aws-cdk-lib@latest dependencies.minimatch'
CVE-2026-26996
